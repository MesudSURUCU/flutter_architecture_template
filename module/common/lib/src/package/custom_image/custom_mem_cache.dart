/// A class that holds custom memory cache dimensions for images.
final class CustomMemCache {
  /// Creates a [CustomMemCache] instance with the given [width] and [height].
  const CustomMemCache({required this.width, required this.height});

  /// The width of the memory cache.
  final int width;

  /// The height of the memory cache.
  final int height;
}
